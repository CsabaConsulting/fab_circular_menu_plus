name: example
description: FAB Circular Menu+ Example
publish_to: 'none'

version: 0.0.2

environment:
  sdk: ">=2.12.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  fab_circular_menu_plus:
    path: ../

dev_dependencies:
  analyzer: ^7.2.0
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
